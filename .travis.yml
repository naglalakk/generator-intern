language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-intern' ]; then cd .. && eval "mv $currentfolder generator-intern" && cd generator-intern; fi
